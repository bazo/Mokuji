1. In index.php change DATA_DIR to absolute path to the directory containing the folder app and libs
2. configure the database in config/config.ini
3. (optional) change the login credentials in config/admin.ini